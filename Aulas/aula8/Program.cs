using aula8.Models;

Calculadora calc = new Calculadora();

calc.Somar(10, 30);
calc.Subtrair(10, 50);
calc.Multiplicar(15, 45);
calc.Dividir(2, 2);
calc.Potencia(3, 3);
calc.Seno(30);
calc.Coseno(30);
calc.Tangente(30);
calc.RaizQuadrada(9);


int numIncremento = 10;

Console.WriteLine(numIncremento);

Console.WriteLine("Incrementando o 10");
numIncremento++;

Console.WriteLine(numIncremento);

int numDecremento = 20;

Console.WriteLine(numDecremento);

Console.WriteLine("Decrementando o 20");
numDecremento--;

Console.WriteLine(numDecremento);



// Operadores Aritmeticos e classe Math

// +  Soma
// - Subtração
// * Multiplicação
// / Divisão
// % Resto
// ++ Incremento
// -- Decremento
// math.pow = potencia
// math.cos, math.sin, math.tan trigonometria - seno, cosseno e tangente
// math.round = arredonda o numero para a quantidade de digitos que definir
